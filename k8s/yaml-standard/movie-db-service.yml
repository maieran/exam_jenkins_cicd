apiVersion: v1
kind: Service

metadata:
  name: jenkins-psql-movie-db


spec:
  type: ClusterIP

  selector:
    app: jenkins-psql-movie-db
  
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432