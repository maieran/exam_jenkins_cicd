apiVersion: v1
kind: Service
metadata:
  name: jenkins-nginx-service
spec:
  type: NodePort

  selector: 
    app: jenkins-nginx-deployment
    
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080