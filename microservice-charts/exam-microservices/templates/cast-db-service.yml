apiVersion: v1
kind: Service

metadata:
  name: jenkins-psql-cast-db


spec:
  type: ClusterIP

  selector:
    app: jenkins-psql-cast-db
  
  ports:
    - name: postgres
      protocol: TCP
      port: {{ .Values.castDatabase.service.port }}
      targetPort: {{ .Values.castDatabase.service.port }}